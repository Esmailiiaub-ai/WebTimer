# WebTimer Project

This project provides a simple web interface for controlling timers.

## Features
- Control multiple timers
- Web-based user interface

## Installation
1. Clone the repository
2. Open `firmware/WebTimer.ino` in the Arduino IDE.
3. Upload the code to your Arduino board.

## Usage
- Access the web interface at `http://<your_ip>/` after uploading the firmware.